# Frozen consensus vectors

The deterministic consensus vectors established during V20.7.x verification are
retained as regression vectors. They are now tested only against the modular
implementation; the former reference implementation is no longer part of the build or test path.
